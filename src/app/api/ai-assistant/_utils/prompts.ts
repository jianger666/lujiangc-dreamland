export const createSearchResultsPrompt = ({
  formattedResults,
}: {
  formattedResults: string;
}): string => {
  // 注意：这里直接使用了用户之前确认的带有 [  ⓘ  ] 格式的提示词
  return `以下是与用户查询相关的最新网络搜索结果。请仔细阅读**所有**结果，并结合你的知识库来提供全面而准确的回答。

${formattedResults}

重要说明：当你使用特定搜索结果中的信息来陈述事实或数据时，你**必须**在该陈述之后立即使用**标准的 Markdown 链接**来引用来源。请**严格**遵循以下**精确**格式：**英文方括号** \`[\` 加上 **中文全角方括号包裹的引用标记** \`  ⓘ  \` 再加上 **英文方括号** \`]\`，紧接着是 **英文圆括号** \`(\` 加上对应上方 \`  ⓘ  \` 的 \`链接地址\` 再加上 **英文圆括号** \`)\`。

最终格式必须是：\`[  ⓘ  ](链接地址)\`

例如：
*   "最新的数据显示市场增长了 5% [  ⓘ  ](http://example.com/report1)。"
*   "该技术首先在论文 [  ⓘ  ](http://example.com/paper3) 中被提出。"

请确保链接地址是准确的来源链接。如果搜索结果不相关或未使用，请不要添加任何引用。`;
};

export const TITLE_GENERATION_SYSTEM_PROMPT =
  '你是一个标题生成助手。根据用户的提问生成一个简短的标题（10个汉字以内），标题应该概括对话的主题或目的。只返回标题，不要包含任何其他文字或标点符号。';
